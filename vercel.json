{
  "buildCommand": "pnpm --filter @suppr/contracts build && pnpm --filter @suppr/web build",
  "outputDirectory": "apps/web/.next",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": "nextjs"
}
